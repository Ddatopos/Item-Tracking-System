# 寻物系统 — 项目架构

---

## 一、整体系统架构

```
┌─────────────────┐     WiFi      ┌──────────────────┐
│  ESP32 + 摄像头  │ ────────────→ │    华为云存储      │
└─────────────────┘               └────────┬─────────┘
                                           │ 上传/下载
                                           ↓
┌─────────────────┐               ┌──────────────────┐
│    处理模块      │ ←──────────── │  图像 + 识别模型   │
│  (输出 CSV)     │               └──────────────────┘
└────────┬────────┘
         │ CSV 文件
         ↓
┌─────────────────────────────────────────────────────┐
│                    寻物逻辑模块（本模块）              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │
│  │  数据处理   │→ │  后端 API   │← │ React 前端  │  │
│  │ (pandas)    │  │ (Flask等)   │  │             │  │
│  └─────────────┘  └──────┬──────┘  └──────┬──────┘  │
│                          │                │         │
│                    HTTP / JSON           用户访问    │
└──────────────────────────┼────────────────┼─────────┘
                           ↓                ↓
                     ┌──────────┐     ┌──────────┐
                     │ 图片存储  │     │   用户   │
                     └──────────┘     └──────────┘
```

---

## 二、寻物模块内部架构

```
camera_demo/
├── backend/                    # 后端
│   ├── app.py                  # 主入口，启动服务
│   ├── config.py               # 配置（CSV 路径、端口等）
│   ├── data/
│   │   ├── processor.py        # 数据处理：读 CSV、筛选、取最后一次出现
│   │   └── recognition.csv     # 处理模块输出的 CSV（或路径可配置）
│   └── requirements.txt
│
├── frontend/                   # React 前端
│   ├── public/
│   ├── src/
│   │   ├── App.jsx
│   │   ├── components/
│   │   │   ├── ItemSelector.jsx   # 物品选择
│   │   │   └── ResultDisplay.jsx  # 结果展示（图片+时间）
│   │   ├── api/
│   │   │   └── itemApi.js         # 调用后端接口
│   │   └── index.jsx
│   ├── package.json
│   └── ...
│
├── images/                     # 图片目录（或由处理模块/华为云路径决定）
│   └── ...
│
└── README.md
```

---

## 三、数据流

```
1. 数据处理
   recognition.csv ──→ processor.py ──→ 按物品分组、按时间排序
                                        ──→ 每个物品取最后一条
                                        ──→ 内存中的数据结构 / 缓存

2. 用户查询
   用户选物品 ──→ React 请求 GET /api/items/{item} ──→ 后端查 processor 结果
                                                     ──→ 返回 { image_path, timestamp }
                                                     ──→ 前端展示图片
```

---

## 四、API 设计（示例）

| 接口 | 方法 | 说明 | 返回示例 |
|------|------|------|----------|
| `/api/items` | GET | 获取所有物品类别列表 | `["手机", "钥匙", "遥控器"]` |
| `/api/items/{item}` | GET | 查询某物品最后一次出现 | `{ "item": "手机", "image_path": "images/xxx.jpg", "timestamp": "2025-01-09 12:00:00" }` |
| `/api/items/{item}/image` | GET | 直接返回图片（可选） | 图片二进制流 |

---

## 五、技术栈总览

| 层级 | 技术 | 职责 |
|------|------|------|
| 数据处理 | Python + pandas | 读 CSV、筛选、整理 |
| 后端 | Flask / FastAPI | 提供 REST API、调用 processor |
| 前端 | React | 物品选择、结果展示 |
| 数据 | CSV + 图片文件 | 识别结果、图片存储 |

---

## 六、部署示意（本地开发）

```
终端 1:  python backend/app.py          # 后端 如 localhost:5000
终端 2:  npm run dev (frontend)         # 前端 如 localhost:3000
```

前端通过配置代理或 CORS 访问后端 API，避免跨域问题。
