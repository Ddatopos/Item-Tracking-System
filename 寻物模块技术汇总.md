# 寻物逻辑模块 + 查询网页 — 技术汇总

> 目标：中期答辩前完成基础功能，实现结项要求

---

## 一、任务概述

**核心功能**：用户选择一个物品类别 → 系统返回该物品**最后一次出现**的照片及位置信息。

**所处流程**：
```
处理模块(CSV) → [本模块] → 查询网页 → 用户
```

---

## 二、需要完成的工作

### 2.1 数据处理部分（Python + pandas）

1. **读取 CSV 数据**  
   - 从处理模块产出的 CSV 文件中读取记录  
   - 需与处理模块同学确认 CSV 列名与格式（见下文“数据格式约定”）

2. **筛选“物品最后一次出现”**  
   - 按**物品类别**分组  
   - 每组内按**时间**排序  
   - 取每组**最后一条**记录（即该物品最后出现的那张图片）

3. **结果整理**  
   - 整理为便于网页展示的结构（如：物品名 → 图片路径/URL + 时间）

### 2.2 网页部分

1. **简易查询网页**  
   - 用户可选择/输入一个物品类别  
   - 点击查询后，显示该物品最后一次出现的图片  
   - 可附带展示：拍摄时间、图片路径等

2. **技术选型**  
   - **前端**：React  
   - **后端**：Flask / FastAPI 提供 API  
   - **数据流**：前端调用后端接口获取物品及图片信息

---

## 三、数据格式约定（需与处理模块确认）

建议与“处理模块”同学约定 CSV 至少包含以下列：

| 列名       | 含义           | 示例           |
|------------|----------------|----------------|
| item/category | 物品类别       | "手机", "钥匙" |
| image_path | 图片路径或 URL | "images/20250109_120000.jpg" |
| timestamp  | 拍摄时间       | "2025-01-09 12:00:00" |

若处理模块输出格式不同，需在本模块中做字段映射。

---

## 四、实现思路

### 4.1 数据处理核心逻辑（pandas 示例）

```python
import pandas as pd

# 1. 读取 CSV（列名需根据实际格式调整）
df = pd.read_csv('识别结果.csv')

# 2. 确保时间列可排序
df['timestamp'] = pd.to_datetime(df['timestamp'])

# 3. 按物品分组，取每组最后一条（即最后一次出现）
last_appearance = df.sort_values('timestamp').groupby('item').last().reset_index()

# 4. 转为字典或 JSON，供网页使用
result = last_appearance.to_dict(orient='records')
```

### 4.2 网页查询流程

```
用户选择物品 → 后端接收请求 → 从 last_appearance 中查该物品 → 返回图片路径+时间 → 前端展示
```

---

## 五、学习内容清单

| 内容           | 用途                     | 推荐资源                         |
|----------------|--------------------------|----------------------------------|
| Python 基础语法 | 写数据处理和简单接口     | 菜鸟教程、廖雪峰 Python 教程     |
| pandas 库      | 读 CSV、筛选、分组、排序 | pandas 官方文档、10分钟入门      |
| Flask / FastAPI | 提供 HTTP 接口给网页     | Flask 官方 Quickstart            |
| React          | 查询页面、展示图片、组件化 | React 官方文档、Create React App |

---

## 六、分工建议（你 + 王琪臻）

| 模块     | 工作内容                         | 可分配 |
|----------|----------------------------------|--------|
| 数据处理 | 读取 CSV、筛选逻辑、生成查询数据 | 一人   |
| 后端 API | Flask/FastAPI 接口、路由、返回 JSON | 一人   |
| 前端网页（React） | 物品选择、查询按钮、图片展示     | 一人   |

建议：两人先一起确认 CSV 格式和接口约定，再分头实现，最后联调。

---

## 七、验收标准（中期答辩）

- [ ] 能正确读取处理模块的 CSV 数据  
- [ ] 能按物品类别筛选出“最后一次出现”的记录  
- [ ] 网页支持选择物品并显示该物品最后一次出现的图片  
- [ ] 能演示完整查询流程  

---

## 八、后续可扩展功能（阶段二、三）

- 展示物品出现历史（多张图）  
- 支持按时间范围筛选  
- 物品类别自动补全/搜索  
- 与华为云图片直接对接（按 URL 展示）  

---

*文档创建日期：2025-02-09*
